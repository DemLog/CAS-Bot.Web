import { createStyles } from "@mantine/core";

export const AdminPageStyles = createStyles((theme) => ({
    searchBoxUser: {
        display: 'flex'
    },
    searchUserItem: {
        flexBasis: '35%'
    },
    userInfoItem: {
        flexBasis: '65%'
    }
}));