import {createStyles} from "@mantine/core";

export const ReportProductListItemStyles = createStyles((theme) => ({
    mainContent: {
        display: 'flex',
        flexDirection: 'column',
        width: '100%'
    }
}));