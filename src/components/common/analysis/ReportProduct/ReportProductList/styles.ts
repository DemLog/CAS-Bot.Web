import {createStyles} from "@mantine/core";

export const ReportProductListStyles = createStyles((theme) => ({
    contentMain: {
        display: 'flex',
        flexDirection: 'column',
        width: '100%',
    }
}));