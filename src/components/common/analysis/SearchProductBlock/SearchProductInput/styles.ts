import {createStyles} from "@mantine/core";

export const SearchProductInputStyles = createStyles((theme) => ({
    mainContent: {
        backgroundColor: '#ddd',
        display: 'flex',
        alignItems: 'center'
    },
    inputContent: {
        flexGrow: 1
    }
}));