import {createStyles} from "@mantine/core";

export const ResultSearchBlockStyles = createStyles((theme) => ({
    mainContent: {
        backgroundColor: '#ddd'
    },
    headerBlock: {
        display: 'flex',
        justifyContent: 'space-between'
    }
}));