import {createStyles} from "@mantine/core";

export const ProfileMobileButtonsStyles = createStyles((theme) => ({
    mainContent: {
        display: 'flex',
        flexDirection: 'column'
    }
}));