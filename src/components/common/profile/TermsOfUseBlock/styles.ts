import {createStyles} from "@mantine/core";

export const ProfileTermOfUseBlockStyles = createStyles((theme) => ({
    commonContent: {
        backgroundColor: '#e3e3e3',
        borderRadius: '8px',
        display: 'flex',
        flexDirection: 'column',
        width: '100%'
    },
    headerBlock: {
        display: 'flex',
        justifyContent: 'space-between'
    }
}));