import {createStyles} from "@mantine/core";

export const UsersActivityListStyles = createStyles((theme) => ({
    tableContent: {
        backgroundColor: '#ddd'
    },
    tdStyle: {
        borderRightColor: '#000 !important'
    },
    thStyle: {
        backgroundColor: '#bebebe'
    }
}));