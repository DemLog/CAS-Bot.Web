import {createStyles} from "@mantine/core";

export const SearchUserInputStyles = createStyles((theme) => ({
    mainContent: {
        border: '1px solid #000'
    },
    inputItem: {
        flexGrow: 1
    }
}));