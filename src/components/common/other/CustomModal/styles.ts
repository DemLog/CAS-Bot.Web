import {createStyles} from "@mantine/core";

export const CustomModalStyles = createStyles((theme) => ({
    mainModal: {
        borderRadius: '8px',
        boxShadow: '0px 0px 15px 1px rgba(0,0,0,0.42)'
    },
    headerTitleModal: {
        position: 'sticky'
    }
}));