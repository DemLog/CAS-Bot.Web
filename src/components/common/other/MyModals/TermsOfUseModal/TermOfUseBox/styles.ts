import {createStyles} from "@mantine/core";

export const TermOfUseBoxStyles = createStyles((theme) => ({
    boxForModal: {
        height: '500px',
        fontSize: '13px',
        border: '2px solid rgba(0,0,0,0.2)',
        borderRadius: '5px'
    }
}));