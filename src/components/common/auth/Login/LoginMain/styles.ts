import {createStyles} from "@mantine/core";

export const LoginMainStyles = createStyles((theme) => ({
    containerMain: {
        display: 'flex',
        alignItems: 'center',
        flexGrow: 1
    },
    containerMainMobile: {
        flexDirection: 'column',
    }
}));