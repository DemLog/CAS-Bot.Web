import {createStyles} from "@mantine/core";

export const HistoryListStyles = createStyles((theme) => ({
    mainContent: {
        display: 'flex',
        flexDirection: 'column'
    },
    headerContent: {
        display: 'flex',
        justifyContent: 'flex-end'
    }
}));